---
const navItems = [
    { name: 'Accueil', href: '/portfolio/' },
    { name: 'Projets', href: '/portfolio/blog' },
    // { name: 'À propos', href: '/portfolio/about' },
];
---

<nav class="sticky top-0 z-50 bg-white/80 supports-[backdrop-filter]:bg-white/40 backdrop-blur-xl backdrop-saturate-150 border-b border-white/40 shadow-sm">
    <ul class="flex justify-center space-x-8 max-w-4xl px-4 mx-auto py-4">
        {navItems.map(item => (
            <li>
                <a 
                  href={item.href} 
                  class="text-gray-900 hover:text-green-600 transition-colors duration-300 text-sm uppercase tracking-wider font-medium"
                >
                  {item.name}
                </a>
            </li>
        ))}
    </ul>
</nav>